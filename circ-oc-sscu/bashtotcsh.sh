
for f in *.sh; do
sed -i -e 's|#!/bin/tcsh|#!/bin/tcsh|' "$f"
done

for f in *.sh; do
  sed -i -e 's|#!/bin/tcsh|#!/bin/tcsh|' "$f"
done

for f in *.sh; do
  sed -i -e 's|#!/bin/tcsh|#!/bin/tcsh|' "$f"
done
